procedure main() 
  local lines, line, i, j, k
  lines := []
  while line := read() do
    put(lines, integer(line))
  if i := 1 to *lines &
     j := i+1 to *lines & 
     k := j+1 to *lines &
     lines[i] + lines[j] + lines[k]= 2020 
  then {
    write(lines[i] * lines[j] * lines[k])
  }
end